# Google Cloud Storage Direct Upload Configuration
FILE_DRIVER=gcs
BUCKET_NAME=
GCS_PROJECT_ID=

# Optional: Leave empty on GCP (Cloud Run, GKE, Compute Engine) to use ADC
GCS_CREDENTIALS=

PRESIGNED_URL_EXPIRY=600
