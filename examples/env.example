# Express Storage - Environment Configuration
# Copy this file to .env and configure based on your storage driver

# =============================================================================
# STORAGE DRIVER (Required)
# =============================================================================
# Options: local, s3, s3-presigned, gcs, gcs-presigned, azure, azure-presigned
FILE_DRIVER=local

# =============================================================================
# LOCAL STORAGE
# =============================================================================
# Path where files will be stored (relative to project root)
LOCAL_PATH=public/express-storage

# =============================================================================
# CLOUD STORAGE COMMON
# =============================================================================
BUCKET_NAME=your-bucket-name
# Default folder path (optional, defaults to root)
# Example: uploads, users/files, data/uploads
BUCKET_PATH=

# =============================================================================
# AWS S3 CONFIGURATION
# =============================================================================
# Required for: s3, s3-presigned
AWS_REGION=us-east-1
AWS_ACCESS_KEY=your-access-key
AWS_SECRET_KEY=your-secret-key

# =============================================================================
# GOOGLE CLOUD STORAGE CONFIGURATION
# =============================================================================
# Required for: gcs, gcs-presigned
# BUCKET_NAME=your-bucket-name
GCS_PROJECT_ID=your-project-id
GCS_CREDENTIALS=path/to/service-account.json

# =============================================================================
# AZURE BLOB STORAGE CONFIGURATION
# =============================================================================
# Required for: azure, azure-presigned
# Option 1: Connection String (recommended)
AZURE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...

# Option 2: Account Name + Key
AZURE_ACCOUNT_NAME=your-account-name
AZURE_ACCOUNT_KEY=your-account-key
AZURE_CONTAINER_NAME=your-container-name

# =============================================================================
# PRESIGNED URL SETTINGS (Optional)
# =============================================================================
# URL expiry time in seconds (default: 600 = 10 minutes)
PRESIGNED_URL_EXPIRY=600

# Maximum file size in bytes (default: 5GB = 5368709120)
MAX_FILE_SIZE=5368709120

# =============================================================================
# SERVER SETTINGS
# =============================================================================
PORT=3000
