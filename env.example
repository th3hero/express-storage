# Storage Driver Configuration
# Set one of the following values: local, s3, s3-presigned, gcs, gcs-presigned, oci, oci-presigned
FILE_DRIVER=local

# Bucket name for cloud storage (required for s3, gcs, oci)
BUCKET_NAME=my-storage-bucket

# Local storage path (for local driver, relative to project root)
LOCAL_PATH=public/express-storage

# Presigned URL expiry time in seconds (default: 600 = 10 minutes)
PRESIGNED_URL_EXPIRY=600

# AWS S3 Configuration (required for s3 and s3-presigned)
AWS_REGION=us-east-1
AWS_ACCESS_KEY=your-aws-access-key
AWS_SECRET_KEY=your-aws-secret-key

# Google Cloud Storage Configuration (required for gcs and gcs-presigned)
GCS_PROJECT_ID=your-gcs-project-id
GCS_CREDENTIALS=path/to/your/gcs-credentials.json

# Oracle Cloud Infrastructure Configuration (required for oci and oci-presigned)
OCI_REGION=us-ashburn-1
OCI_CREDENTIALS=path/to/your/oci-credentials.json 