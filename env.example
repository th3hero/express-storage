# Storage Driver Configuration
# Set one of: local, s3, s3-presigned, gcs, gcs-presigned, azure, azure-presigned
FILE_DRIVER=local

# Bucket/Container name for cloud storage (required for s3, gcs, azure)
BUCKET_NAME=

# Default folder path for cloud storage (optional, defaults to root)
# Example: uploads, users/files, data/uploads
BUCKET_PATH=

# Local storage path (for local driver, relative to project root)
LOCAL_PATH=public/express-storage

# Presigned URL expiry time in seconds (default: 600 = 10 minutes)
PRESIGNED_URL_EXPIRY=600

# Maximum file size in bytes (default: 5GB = 5368709120)
MAX_FILE_SIZE=5368709120

# AWS S3 Configuration (required for s3 and s3-presigned drivers)
AWS_REGION=
# Optional: Leave empty on AWS to use IAM Role credentials
AWS_ACCESS_KEY=
AWS_SECRET_KEY=

# Google Cloud Storage Configuration (required for gcs and gcs-presigned drivers)
GCS_PROJECT_ID=
# Optional: Leave empty on GCP to use Application Default Credentials (ADC)
GCS_CREDENTIALS=

# Azure Blob Storage Configuration (required for azure and azure-presigned drivers)
# Option 1: Connection string (recommended - get from Azure Portal)
AZURE_CONNECTION_STRING=
# Option 2: Account credentials (alternative to connection string)
AZURE_ACCOUNT_NAME=
AZURE_ACCOUNT_KEY=
# Container name (optional - uses BUCKET_NAME if not set)
AZURE_CONTAINER_NAME=